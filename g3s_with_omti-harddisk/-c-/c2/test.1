const SortZahl  = 500;
      StrLength =   40;

type SortType = String[StrLength];

var SortArray : array[1..SortZahl] of SortType;

function Kleiner(var x,y : SortType) : boolean;
  begin Kleiner := x<y end;

function groesser(var x,y : sorttype) : boolean;
  begin groesser := x>y end;

procedure zufallsfeld;
  var i,j : integer;
  begin
    writeln('Das Feld wird mit Zufallsstrings gefuellt!');
    for i:=1 to sortzahl do begin
      sortarray[i][0]:=chr(strlength);
      for j:=1 to strlength do sortarray[i][j]:=chr(65+random(26))
    end (* for i *)
  end;  (* ZufallsFeld *)
